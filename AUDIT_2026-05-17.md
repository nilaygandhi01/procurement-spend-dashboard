# Full codebase audit — 2026-05-17

Scope: Git repo root `procurement-spend-dashboard/procurement-spend-dashboard` (inner folder).  
Outer sibling folder `…/procurement-spend-dashboard/` (parent of this repo) is **outside** this clone; paths noted where relevant.

---

## Phase 1 — Findings (report only)

### 1. JavaScript syntax

| Artifact | Method | Result |
|----------|--------|--------|
| `src/dashboard/harmonization-client.js` | `node --check` | **OK** |
| `src/dashboard/index.html` early inline block (global errors, ~L1764–1832) | `node --check` on extracted body | **OK** |
| `src/dashboard/index.html` `D = {…}` block (~L3208–3225) | `node --check` | **OK** |
| `src/dashboard/index.html` shell wiring IIFE (~L3228–3309) | `node --check` | **OK** |
| `src/dashboard/index.html` main bundle (`startProcurementDashboard` … end) | Lines **3312–14710** written to temp file, `node --check` | **OK** |

**Note:** A naive regex “split on `<script>`” is **unsafe** here because HTML `src="..."` uses `src=` not `src\s*=` immediately after `src`, so a lookahead meant to skip external scripts can fail; validation used **known line ranges** and `harmonization-client.js` directly.

**Errors with line numbers:** none detected by `node --check`.

### 2. Python syntax

| Path | `python -m py_compile` |
|------|-------------------------|
| `src/scripts/refresh_data.py` | OK |
| `src/scripts/build_spend_data.py` | OK |
| `src/scripts/harmonization.py` | OK |
| `src/scripts/rap_category_filter_mapping.py` | OK |
| `src/scripts/verify_rap_category_baselines_2025.py` | OK |

**Errors:** none.

### 3. Dead code / duplicates / noise

| Item | Assessment |
|------|------------|
| **Outer** `…/procurement-spend-dashboard/Cummins_IDP_Dashboard.html`, `Cummins_IDP_Dashboard.BACKUP.html` | **Outside** this Git repo. Legacy/alternate entrypoints; not referenced by inner CI (`publish = src/dashboard`). **Flag for manual cleanup** (do not delete from this audit run). |
| **Outer** `harmonization.py`, `refresh_data.py`, `build_spend_data.py` | **Outside** repo. **Outer `harmonization.py` differs** from `src/scripts/harmonization.py` (byte size mismatch when compared). Risk: running Python from the **parent** folder can use **stale** logic vs CI (`python src/scripts/refresh_data.py`). **Flag:** prefer documented canonical command only. |
| **`harmonization-client.js`** | Inner canonical: `src/dashboard/harmonization-client.js`. Compared to outer copy on disk: **identical** (same size and bytes). |
| **`index.html` (repo root)** | Redirect stub → `src/dashboard/index.html`. OK as entry shim. |
| **`__pycache__/` / `*.pyc`** | Listed in `.gitignore`; should not be committed. |

### 4. Data contract (`data.json`)

**Expected shape** (from `src/scripts/refresh_data.py` header and `applyPayloadToD` / `loadProcurementData` in `index.html`):

- Preferred: **object** with non-empty **`rows`** array; optional **`harmonization_results`** (or legacy **`harmonization_data`**); optional **`ytd_comparison`**, **`monthly_spend_by_type`**.
- Legacy: **top-level array** of row objects (optional harmonization attachment patterns documented in code).

**Row fields** after `normalizeRowForD`: aligns with Python-enriched flat rows (`part`, `supplier`, `spend`, `quantity`/`qty`, `price`, `spend_type`, categories, `ym`, `year`, etc.).

**Mismatch risk:** `.gitignore` excludes typical `data.json` artifacts; **no committed sample** was validated in this run against a live payload. **Recommendation:** run `python src/scripts/refresh_data.py` locally and smoke-test load + Harmonization tab once.

### 5. Tab entry points — `try/catch`

| Tab | Before audit | After Phase 2 critical fix |
|-----|----------------|-----------------------------|
| Overview | Chart `resize` wrapped in per-chart `try/catch` | Unchanged |
| Part Search | **No** wrapper around `syncPartSearchAfterFilter` | **Wrapped** |
| RAP | **No** wrapper around `renderRapTab` | **Wrapped** |
| Harmonization | **No** wrapper around populate + `renderHarmonizationUnified` | **Wrapped** |
| Single Source | **No** wrapper around `idpRenderSingleSourceView` | **Wrapped** |
| Index Analysis | Already guarded (`renderIndexAnalysisView`, `withLoad`) | Unchanged |
| Price Explanation | **No** wrapper around PE render calls | **Wrapped** |
| Cleansheet (fallback branch) | `renderCleansheet` in `try/catch` | Unchanged |

Global `window.onerror` / `unhandledrejection` handlers exist early in `index.html`; tab wrappers reduce risk of leaving the UI half-switched when a render throws.

### 6. Performance — synchronous hot loops

| Area | Notes |
|------|--------|
| **`internEnrichedBundleToD`** | Full passes over **all rows** for interning on load — **required** for cube; no chunking (acceptable as one-time cost; overlay covers it). |
| **`getFiltered()`** | Can scan **O(n)** on large `D.n`; inherent to filtering unless incremental index added — **flag only**, not changed. |
| **Index Analysis parts catalog** | Uses caps / virtual scroll / batched work per prior design — **flag:** spot-check with max dataset in browser. |
| **`harmonization-client.js`** | Uses caps (`MAX_CLIENT_HARM_INPUT_ROWS`, `MAX_ALL_OPPORTUNITY_CARDS`, `HARM_COMPUTE_BUDGET_MS`) — OK by inspection. |

### 7. Console errors (load + tab navigation)

**Not executed:** no headless browser run in this audit.  
**Recommendation:** open `src/dashboard/index.html` served locally, confirm clean console on load and each tab switch; optional `?idpdiag=1` per HTML comment.

---

## Phase 2 — Critical fixes applied

Fixed: Wrapped Part Search tab activation (`syncPartSearchAfterFilter`) in try/catch in src/dashboard/index.html:12739  
Fixed: Wrapped RAP tab activation (`readFiltersFromForm` + `renderRapTab`) in try/catch in src/dashboard/index.html:12760  
Fixed: Wrapped Harmonization tab activation (`harmUnifiedPopulateCategoryFilters` + conditional `renderHarmonizationUnified`) in try/catch in src/dashboard/index.html:12779  
Fixed: Wrapped Single Source tab activation (`readFiltersFromForm` + `idpRenderSingleSourceView`) in try/catch in src/dashboard/index.html:12812  
Fixed: Wrapped Price Explanation tab activation (`readFiltersFromForm` + `idpPeRenderAnalyzingList` + `idpPeRefreshIfActive`) in try/catch in src/dashboard/index.html:12886  

---

## Phase 3 — Staging (stopped before commit)

_Staging results appear in the assistant’s final message after `git add -A`._

**User instruction:** do **not** commit or push until explicit approval after reviewing staged set.
